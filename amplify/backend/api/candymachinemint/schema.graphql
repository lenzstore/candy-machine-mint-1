type WhitelistAddress @model @auth(rules: [{allow: public}]) {
  id: ID!
  wallet: String!
  count: Int!
}

type MintCount @model @auth(rules: [{allow: public}]) {
  id: ID!
  wallet: String
  count: Int
}
 